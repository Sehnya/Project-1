import React from "react";
import Workspace from "./components/Workspace"; // Import the Workspace component

function App() {
    return (
        <div>
            <Workspace /> {/* Render the Workspace component */}
        </div>
    );
}

export default App;